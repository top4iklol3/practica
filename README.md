# Облачное хранилище файлов

Веб-приложение для хранения и управления файлами, похожее на облачные диски (Google Drive, Dropbox).

## Возможности

- 📤 **Загрузка файлов** - загружайте один или несколько файлов одновременно
- 📥 **Скачивание файлов** - скачивайте файлы одним кликом
- 📁 **Создание папок** - создавайте папки для организации файлов
- 🗂️ **Навигация по папкам** - переходите между папками, используйте breadcrumb навигацию
- 🗑️ **Удаление** - удаляйте файлы и папки
- 📱 **Адаптивный дизайн** - работает на всех устройствах

## Требования

- .NET 8.0 SDK или выше
- PostgreSQL 12 или выше

## Установка и запуск

1. Убедитесь, что у вас установлен .NET 8.0 SDK:
   ```bash
   dotnet --version
   ```

2. Установите и запустите PostgreSQL:
   
   **Быстрая установка:**
   ```powershell
   .\install-postgresql.ps1
   ```
   
   **Или вручную:**
   - См. подробную инструкцию в [INSTALL_POSTGRESQL.md](INSTALL_POSTGRESQL.md)
   - Скачайте и установите PostgreSQL с [официального сайта](https://www.postgresql.org/download/windows/)
   - После установки запустите службу PostgreSQL
   - База данных `filestorage` будет создана автоматически при первом запуске приложения

3. Настройте строку подключения в `appsettings.json`:
   ```json
   "ConnectionStrings": {
     "DefaultConnection": "Host=localhost;Port=5432;Database=filestorage;Username=postgres;Password=ваш_пароль"
   }
   ```

4. Восстановите зависимости:
   ```bash
   dotnet restore
   ```

5. Запустите приложение:
   ```bash
   dotnet run
   ```
   
   База данных будет создана автоматически при первом запуске.

6. (Опционально) Проверьте подключение к БД:
   ```powershell
   .\check-db.ps1
   ```

7. Запустите приложение:
   ```bash
   dotnet run
   ```
   
   База данных будет создана автоматически при первом запуске.

8. Откройте браузер и перейдите по адресу:
   - Frontend: `http://localhost:5000` или `https://localhost:5001`
   - Swagger API: `http://localhost:5000/swagger` или `https://localhost:5001/swagger`

## Решение проблем с подключением к БД

Если вы видите ошибку подключения к PostgreSQL:

### 1. Проверьте, что PostgreSQL запущен

**Windows:**
```powershell
# Проверьте службу PostgreSQL
Get-Service -Name postgresql*

# Если служба не запущена, запустите её
Start-Service -Name postgresql*
```

**Linux/Mac:**
```bash
# Проверьте статус
sudo systemctl status postgresql

# Запустите, если не запущен
sudo systemctl start postgresql
```

### 2. Проверьте строку подключения

Убедитесь, что в `appsettings.json` указаны правильные параметры:
- **Host** - обычно `localhost` или `127.0.0.1`
- **Port** - обычно `5432` (проверьте в настройках PostgreSQL)
- **Database** - имя базы данных (может быть создана автоматически)
- **Username** - имя пользователя PostgreSQL (обычно `postgres`)
- **Password** - пароль пользователя

### 3. Проверьте доступность порта

```powershell
# Windows
Test-NetConnection -ComputerName localhost -Port 5432

# Linux/Mac
nc -zv localhost 5432
```

### 4. Создайте базу данных вручную (если нужно)

Подключитесь к PostgreSQL и выполните:
```sql
CREATE DATABASE filestorage;
```

Или используйте psql:
```bash
psql -U postgres -c "CREATE DATABASE filestorage;"
```

### 5. Используйте скрипт проверки

Запустите скрипт диагностики:
```powershell
.\check-db.ps1
```

### 6. Альтернативная строка подключения

Если PostgreSQL слушает на IPv4, попробуйте:
```json
"DefaultConnection": "Host=127.0.0.1;Port=5432;Database=filestorage;Username=postgres;Password=ваш_пароль"
```

Вместо `localhost` используйте `127.0.0.1`.

## Структура проекта

```
FileStorage/
├── Controllers/
│   └── FileStorageController.cs  # API контроллер
├── Data/
│   └── FileStorageDbContext.cs   # DbContext для работы с БД
├── Models/
│   └── StorageItem.cs            # Модель данных для файлов и папок
├── wwwroot/                      # Статические файлы (frontend)
│   ├── index.html               # Главная страница
│   ├── style.css                # Стили
│   └── app.js                   # JavaScript логика
├── Storage/                      # Папка для хранения файлов (создается автоматически)
├── Program.cs                    # Точка входа
└── FileStorage.csproj           # Файл проекта
```

## API Endpoints

- `GET /api/FileStorage/list?path=` - Получить список файлов и папок
- `POST /api/FileStorage/upload?path=` - Загрузить файл(ы)
- `GET /api/FileStorage/download?path=` - Скачать файл
- `POST /api/FileStorage/folder?path=` - Создать папку
- `DELETE /api/FileStorage/item?path=` - Удалить файл или папку

## Использование

1. **Загрузка файлов**: Нажмите кнопку "Загрузить файл" и выберите файлы
2. **Создание папки**: Нажмите "Создать папку", введите название и подтвердите
3. **Открытие папки**: Кликните на папку, чтобы открыть её
4. **Скачивание файла**: Кликните на файл или нажмите кнопку "Скачать"
5. **Удаление**: Нажмите кнопку "Удалить" рядом с файлом или папкой
6. **Навигация**: Используйте breadcrumb вверху или кнопку "Назад" для навигации

## Безопасность

⚠️ **Важно**: Это базовое приложение для локального использования. Для продакшена необходимо добавить:
- Аутентификацию и авторизацию
- Ограничения на размер файлов
- Валидацию типов файлов
- Защиту от path traversal атак (частично реализовано)
- Логирование и мониторинг

## Лицензия

MIT

